Hi,

Thank you for the detailed report — it helped us pinpoint the issue quickly.

We've identified and fixed the root cause of the 401 "Invalid or expired API key" error. There were a few issues on our end:

1. A database column that stores API keys was too short (64 characters) for the actual key length (68 characters). This caused keys to either fail to save or get silently truncated, making authentication impossible regardless of how you sent the key.

2. The X-API-Key header was not being properly forwarded to PHP on our hosting environment, so even correctly generated keys weren't reaching the authentication layer.

3. A whitespace-handling issue in our header parsing could cause hash mismatches.

All of these have been patched and deployed.

WHAT YOU NEED TO DO:

1. REGENERATE YOUR API KEY
   - Log into your dashboard
   - Go to Settings > API Keys
   - Revoke your existing key(s)
   - Generate a new API key
   - Copy and store it securely — it won't be shown again

2. TEST WITH THE NEW DIAGNOSTIC ENDPOINT
   We've added a new endpoint specifically for verifying your API key setup:

   curl -X GET https://app.adverseforms.com/api/v1/auth/verify \
     -H "Authorization: Bearer YOUR_NEW_API_KEY"

   If everything is working, you'll get:
   {
     "authenticated": true,
     "message": "API key is valid.",
     "key_name": "...",
     "scopes": [...]
   }

   If something is still wrong, the response will tell you exactly what the issue is (missing header, invalid key, etc.) along with hints to fix it.

3. UPDATE YOUR INTEGRATION
   Replace the old API key with the new one in your application code. Both header methods work:

   Authorization: Bearer adv_xxxx...
   X-API-Key: adv_xxxx...

4. RETRY YOUR ORIGINAL REQUESTS
   Once /v1/auth/verify returns success, all other endpoints (balance, send, contacts, etc.) will work with the same key.

QUICK TEST COMMANDS:

   # Check balance
   curl -X GET https://app.adverseforms.com/api/v1/sms/balance \
     -H "Authorization: Bearer YOUR_NEW_API_KEY"

   # Send a test SMS
   curl -X POST https://app.adverseforms.com/api/v1/sms/send \
     -H "Authorization: Bearer YOUR_NEW_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"to": "0241234567", "message": "Test from API", "sender_id": "missionhope"}'

If you still encounter any issues after regenerating your key, please send us the full response body from the /v1/auth/verify endpoint and we'll investigate further.

Apologies for the inconvenience, and thank you for your patience.

Best regards,
Adverse Forms Support


================================================================================
AI ASSISTANT PROMPT — FIXING ADVERSE FORMS SMS API AUTHENTICATION
================================================================================

If you are an AI coding assistant helping a developer integrate with the
Adverse Forms SMS API, read the context below and follow the instructions.

CONTEXT:
The developer previously received 401 "Invalid or expired API key" errors on
all SMS API requests. The Adverse Forms team has now fixed the server-side bugs
(database column length, header forwarding, whitespace parsing). The developer
must regenerate their API key and update their code.

WHAT YOU NEED TO DO:

1. Find everywhere in the codebase where the old Adverse Forms SMS API key is
   stored or referenced. It will look like: adv_ followed by 64 hex characters
   (68 characters total). Common locations:
   - .env files (e.g. ADVERSE_API_KEY=adv_...)
   - Config files (config.js, config.php, settings.py, etc.)
   - Hardcoded in source files (look for "adv_" or "Bearer adv_")

2. Replace the old key with the new one the developer has generated from the
   Adverse Forms dashboard (Settings > API Keys). The developer should paste
   the new key when prompted.

3. Make sure the API key is sent in requests using ONE of these header formats:
   - Authorization: Bearer adv_xxxxxxxxxxxx...
   - X-API-Key: adv_xxxxxxxxxxxx...
   Do NOT put the key in the URL query string for production use.

4. Verify the base URL is correct:
   - Production: https://app.adverseforms.com/api/v1/
   - All SMS endpoints are under: /v1/sms/...

5. Test the fix by calling the diagnostic endpoint:

   GET /v1/auth/verify
   Header: Authorization: Bearer <NEW_API_KEY>

   Expected success response:
   {
     "authenticated": true,
     "message": "API key is valid.",
     "key_name": "...",
     "scopes": [...]
   }

6. If /v1/auth/verify returns success, proceed to test the actual SMS endpoints:

   GET  /v1/sms/balance              — Check SMS credit balance
   POST /v1/sms/send                 — Send a single SMS
   POST /v1/sms/send/bulk            — Send bulk SMS
   GET  /v1/sms/messages             — List sent messages
   GET  /v1/sms/sender-ids           — List sender IDs
   POST /v1/sms/sender-ids           — Register a new sender ID
   GET  /v1/sms/contacts/groups      — List contact groups
   POST /v1/sms/contacts             — Add contacts

7. Example send request (adapt to the developer's language/framework):

   POST https://app.adverseforms.com/api/v1/sms/send
   Headers:
     Authorization: Bearer <API_KEY>
     Content-Type: application/json
   Body:
   {
     "to": "0241234567",
     "message": "Hello from the API",
     "sender_id": "missionhope"
   }

8. Common errors and fixes:
   - 401 "Invalid or expired API key" → Key is wrong or old. Regenerate it.
   - 401 "No API key found"           → Header is not being sent. Check your
                                         HTTP client configuration.
   - 403 "Insufficient scope"         → Key doesn't have SMS permissions.
                                         Generate a new key with SMS scope.
   - 422 "Sender ID not approved"     → Sender ID needs admin approval first.

IMPORTANT: The old API key will NOT work. The developer MUST generate a new one
from the dashboard before any API calls will succeed.
================================================================================
